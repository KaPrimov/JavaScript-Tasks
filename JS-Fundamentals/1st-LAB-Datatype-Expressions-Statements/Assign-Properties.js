function assignProp([prop1, value1, prop2, value2, prop3, value3]) {
    let myObj = {};
    myObj[prop1] = value1;
    myObj[prop2] = value2;
    myObj[prop3] = value3;

    return myObj

}

assignProp(['ssid', '90127461', 'status', 'admin', 'expires', '600']);